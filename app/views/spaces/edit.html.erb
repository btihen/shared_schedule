<p hidden id='edit-space-<%= space.id %>' class='pageName'>Edit Space <%= space.id %></p>

<h1>Editing Space</h1>

<%= render 'form', space: @space %>

<%= link_to 'Show', @space %> |
<%= link_to 'Back', spaces_path %>
