<p hidden id='edit-resevation-<%= reservation.id %>' class='pageName'>Edit Reservation <%= reservation.id %></p>

<h1>Edit Reservation</h1>
<%= form_for( reservation, local: true,  # PATH MUST BE SINGULAR!!!
              url: tenant_space_reservation_path(tenant, space, reservation),
              id: "edit-reservation-form", class: "reservation" ) do |form|  %>

  <%= render 'form', form: form, reservation: reservation, event: event, space: space, tenant: tenant %>

<% end %>
