<p hidden id='new-resevation' class='pageName'>New Reservation</p>

<h1>New Reservation</h1>

<%= form_for( reservation, local: true,
              url: tenant_space_reservations_path(tenant, space) # (tenant_id: tenant.id, space_id: space.id, id: reservation.id),
              id: "new-reservation-form", class: "reservation" ) do |form|  %>

  <%= render 'form', form: form, reservation: reservation, event: event, space: space, tenant: tenant %>

<% end %>

<%# link_to 'Back', tenant_path(tenant, date: Calendar.display_date(date)) %>
